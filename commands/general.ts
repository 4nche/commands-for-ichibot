import register from "../register";

register.alias('pl', 'position list',);
register.alias('al', 'alias list',);
register.alias('lp', 'check value lastprice');
register.alias('lb', 'check value bidprice',);
register.alias('la', 'check value askprice');
register.alias('spr', 'check value askprice-bidprice');
register.alias('accud', 'check value accountvalueusd');
